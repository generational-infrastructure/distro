#!/usr/bin/env bash
# Used by https://direnv.net

# Automatically reload when the flake/dev shell inputs change
watch_file flake.nix flake.lock devshell.nix formatter.nix treefmt.nix

# Load `nix develop`
use flake

# Extend the environment with per-user overrides
source_env_if_exists .envrc.local
