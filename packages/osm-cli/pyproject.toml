[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "osm-cli"
version = "0.1.0"
description = "OpenStreetMap CLI for search, nearby, and routing"
license = "MIT"
requires-python = ">=3.10"

[project.scripts]
osm-cli = "osm_cli:main"
